<h5>カテゴリー一覧</h5>
<div class="ml-5">
  <% @categories.each do |category| %>
    <%= link_to category_path(category), class: 'mr-4' do %>
        <%= category.category_name %>
    <% end %>
  <% end %>
</div>
